## 1. Freeze complete pre-implementation RED coverage

- [x] 1.1 In `repos/arashi`, add failing source-policy and generated-contract tests for canonical `arashi`, alias `aw`, “Arashi Workspace,” npm bin mappings, channel-specific launcher/native-binary sets, managed markers plus versioned ledger schema/name, canonical branding boundaries, exact package/release/checksum/retained-archive/installer consumers, exact-version post-publication gate registration, and the rule that `aw` is absent from Commander command paths and `aliasPaths`; record focused RED before creating production policy or artifacts.
- [x] 1.2 Add failing packed-npm process tests that install the real archive in clean POSIX and Windows fixtures and invoke both generated shims for version/help, missing-binary first use, explicit `install`, representative human/JSON success and failure, and wrapper-intercepted update conflicts/planning/application; require matching exit/output/mutation contracts and clean fixture teardown before changing `package.json` or `bin/arashi.js`.
- [x] 1.3 Add failing direct-wrapper and POSIX installer tests for checksummed `aw`, stable ownership markers, the versioned path/hash ownership ledger, one native binary, fresh/pre-alias/managed/manual/partial installs, malformed/mismatched ledger and unmarked regular-file/directory/symlink/unreadable destination collisions, `command -v aw` collisions outside the selected directory without execution, refusal before downloads/directory creation/backups, complete payload-plus-ledger replacement/smoke/commit rollback, rollback failure, unique transaction sidecars, both-version equality, update mode, PATH/shell deferral, and retained archive membership before editing wrappers, release metadata, or `install.sh`.
- [x] 1.4 Add failing Windows PowerShell 5.1 unit/transaction/release-contract tests for the seven-file installed executable payload plus versioned path/hash ownership ledger, all three marked alias assets, absent/pre-alias/managed/manual/unmarked/malformed-ledger/directory/reparse-point collisions, pre-existing PowerShell/CMD/Git Bash `aw` resolution outside the selected directory without execution, refusal before downloads/directory creation/backups, replacement/dual-smoke/ledger-commit rollback, rollback failure, deferred update, canonical manual asset set, and complete cleanup before editing Windows wrappers or `install.ps1`.
- [x] 1.5 Extend the canonical default-Windows-installer acceptance harness first so an isolated default installation must persist PATH, resolve exact installer-produced `arashi` and `aw` entrypoints in separately launched Git Bash, PowerShell, and Command Prompt processes, compare both outputs exactly with the deterministic fixture binary, prove no profile edits, and restore PATH/profile/temp state on every outcome; record native Windows RED.
- [x] 1.6 Add failing shell-integration tests before renderer changes for deterministic dual-name Bash/Zsh/Fish functions, corresponding `command` bypasses, preservation without execution of unrelated parent-shell `aw` aliases/functions, acceptance of direct and npm-shim executable resolution, successful definition after deliberate collision removal and re-source, directive cleanup/escaping/exit status, byte-identical init output through both executables, old-block upgrade, repeated single-block installation, and a real `switch --cd` caller-directory change through `aw` in each shell without inventing create-side directives.
- [x] 1.7 Add failing generated and real-shell completion tests before renderer regeneration requiring Bash, Zsh, and Fish registration for both direct and wrapper-function names, identical static/dynamic/special-character candidates, canonical backend query without recursion, source/compiled/npm-first-use parity, non-mutation, and freshness failure when either name drifts.
- [x] 1.8 In `repos/arashi-docs`, register a focused executable-alias semantic checker through the stable docs aggregate and add controlled RED fixtures for concise alias guidance, canonical identity, npm/POSIX/Windows channel claims, destination/PATH/shell-namespace collisions, manual-wrapper-to-ledger migration guidance, dual-name shell/completion behavior, complete Windows manual assets, landing-page concision, and generated Markdown/`llms.txt`/`llms-full.txt` parity before editing prose or exports.
- [x] 1.9 In `repos/arashi-skills`, register a focused executable-alias checker through stable source/package aggregates and add controlled RED fixtures before guidance edits for “Arashi Workspace,” canonical entry/help commands, smallest-reference ownership, no duplicate command vocabulary, authored/extracted archive parity, and deliberate drift.
- [x] 1.10 In the meta repository, register a focused executable-distribution checker and add RED fixtures before child implementation for typed artifact shape, exact package/release/install sets, one-native-binary routing, ledger-backed markers/collision policy, canonical companion exclusions, docs/export/skill agreement, deliberate semantic mismatches, and executable reachability through existing source/package/meta aggregates without feature-specific workflow steps.
- [x] 1.11 Before further installer production edits, add focused RED acceptance for POSIX filesystem-command classification that ignores exported aliases/functions, physical identity through a symlinked PATH directory, transaction-scoped `HUP`/`INT`/`TERM` and abnormal-exit rollback after mutation, and Windows selection of verified Git for Windows Bash plus native path conversion when WSL/Cygwin/unrelated Bash wins ordinary PATH; capture native Windows RED for the Windows path-selection case.

## 2. Implement CLI-owned executable distribution

- [x] 2.1 Add one typed executable-entrypoint policy and deterministic versioned contract generator/artifact in `repos/arashi`; validate canonical/alias identity, package and direct-launcher sets, native binary sharing, markers, shell names, concrete producer equality, generation idempotency, and command-contract exclusion.
- [x] 2.2 Map npm bins `arashi` and `aw` to the same `bin/arashi.js`, preserve direct-invocation detection, first-use stdout isolation, `install`/`update` interception, and canonical errors, then make the packed-package process matrix GREEN on POSIX and Windows.
- [x] 2.3 Add marked `aw`, `aw.ps1`, and `aw.bat` launchers that preserve platform selection, symlink/path resolution where applicable, argv, exit status, interactive stdin, and existing conditional stdin behavior while delegating to one adjacent canonical native binary.
- [x] 2.4 Update package-content expectations and generated executable contract artifacts, prove npm archives contain only the required entrypoint/release support files, and keep the config schema plus Commander command contract semantically unchanged.

## 3. Implement collision-safe recoverable direct installation

- [x] 3.1 Refactor the POSIX installer to preflight destination/PATH/ledger ownership before downloads or directory creation, download/checksum canonical and alias wrappers, transact `arashi.bin`/`arashi`/`aw` plus the atomic versioned ownership ledger with complete backup and exact rollback, smoke-test identical versions through both names, preserve unrelated paths, and delay PATH/shell changes until commit.
- [x] 3.2 Extend Windows release/install constants and the PowerShell payload transaction to all seven installed executable files plus the atomic versioned ownership ledger, preflight destination/PATH/ledger ownership before downloads or directory creation, smoke-test canonical and alias entrypoints with identical output, preserve recoverable rollback/deferred-update behavior, and keep persistent user-PATH/no-profile policy unchanged.
- [x] 3.3 Update checksum generation, semantic-release uploads, retained platform archive packaging, release-contract exact-set tests, manual package README content, and all installer fallback asset lists from the source-owned distribution policy; add tested `pnpm release:verify-aw -- <version>` public npm/POSIX acceptance plus a same-version manual-dispatch native Windows stage, rejecting missing/`latest`/unpublished versions; do not publish a second native binary.
- [x] 3.4 Run focused POSIX installer fresh/upgrade/collision/replacement/smoke/rollback/update acceptance and native Windows PowerShell 5.1 transaction/default-installer matrices from the stable tree; reconcile only product or harness defects exposed by those pre-existing fixtures.
- [x] 3.5 After task 1.11 RED evidence is committed, make POSIX preflight compare only filesystem-backed commands by physical identity, add transaction-scoped termination/abnormal-exit rollback through ledger commit and backup removal, make Windows collision probing select verified Git for Windows Bash and convert its result natively, then rerun focused POSIX and native Windows matrices before resolving the related review threads.

## 4. Implement dual-name shell integration and completion

- [x] 4.1 Generate deterministic Bash, Zsh, and Fish parent-shell programs defining `arashi` plus a collision-guarded `aw` function with corresponding real-command bypass, shared directive safety, exact cleanup, and exit propagation; preserve unrelated parent-shell aliases/functions without executing them, retain one canonical managed block and activation pair, and make runtime plus release-installer block upgrades idempotent.
- [x] 4.2 Register the one generated completion model for both names in Bash, Zsh, and Fish, accept either root token while using the canonical internal query backend, regenerate embedded completion artifacts, and preserve descriptions, boundaries, budget, silent failure, and non-mutation.
- [x] 4.3 Run the complete real Bash/Zsh/Fish matrix for direct and wrapper invocation, a real `switch --cd` caller-directory change without adding create-side directives, static/dynamic completion, shell-sensitive values, old-block migration, repeated installation, compiled binaries, and packed npm first use through both names.

## 5. Synchronize canonical docs and coordinated contracts

- [x] 5.1 After the docs checker is RED, keep the landing page free of a separate alias-identity paragraph and describe `aw` only as a shorter alias for `arashi` in getting-started guidance; update maintained installation, update, shell, completion, manual Windows payload, collision, and troubleshooting guidance without expanding the landing install tab into implementation detail.
- [x] 5.2 Regenerate canonical Markdown routes, `/llms.txt`, and `/llms-full.txt`; run the docs semantic aggregate and deliberate-mismatch self-tests against authored and generated output, and verify tracked export ownership and deterministic regeneration.
- [x] 5.3 After the skill checker is RED, add concise alias identity/availability guidance to the smallest linked installation/tutorial reference while preserving canonical `arashi` entry/help commands and minimal `SKILL.md`; make authored and extracted canonical-package aggregates GREEN.
- [x] 5.4 Implement the registered meta executable-distribution checker against the generated CLI artifact and normalized docs/export/skill evidence, encode explicit canonical-only VS Code and Commander-command decisions, make source/package/meta aggregate acceptance GREEN, and leave stable workflow topology unchanged unless an independently verified reachability defect requires it.
- [x] 5.5 Run CLI repository-local contract/completion/package/release gates, docs source/generated aggregate gates, skills source/canonical-package aggregate gates, and the complete coordinated contract aggregate once from final child artifacts; reject any success that depends on ignored local generated files.

## 6. Review and deliver child repositories

- [x] 6.1 Before each child commit, review the exact staged diff against every delta scenario and surrounding producer/consumer, run final focused plus full repository format/lint/typecheck/test/build gates after the last edit, verify unchanged lockfiles and unrelated children, and commit CLI, docs, and skills changes independently.
- [x] 6.2 Run independent latest-snapshot spec-compliance and code/content-quality reviews for each changed child, reconcile only concrete approved-contract blockers, rerun affected final gates, and preserve the bounded review cutoff.
- [x] 6.3 Push the CLI, docs, and skills child branches normally, open cross-linked PRs that track issue #290 without closing it, verify exact-head CI including native Windows and package/release plus authored/extracted-skill matrices, and merge green child PRs before the meta proposal PR.

## 7. Verify the published release and prepare archive

- [x] 7.1 Publish the additive CLI release through the normal release workflow and verify its exact assets/checksums plus public npm archive; do not treat merged source or a locally assembled payload as release evidence.
- [x] 7.2 Run `pnpm release:verify-aw -- <exact-version>` from the stable CLI tree to verify the public npm package and official pinned POSIX installers with `arashi` and `aw`, including first use, version equality, representative human/JSON behavior, shell integration, and completion; retain its exact-version evidence.
- [x] 7.3 Dispatch the native Windows post-publication stage with that same exact version and verify the published official PowerShell installer and public assets in fresh Git Bash, PowerShell, and Command Prompt processes with exact `arashi`/`aw` resolution and identical pinned-version output.
- [x] 7.4 Update OpenSpec task evidence only after the related implementation, child PR, CI, merge, and release gates are complete; run strict OpenSpec validation, final cross-repository aggregates, `git diff --check`, exact artifact-hash review, and confirm every pre-archive task is truthfully complete so the change is ready for archive/sync and final meta merge.
