## 1. Baseline and acceptance trace

- [x] 1.1 Re-read issue #271, the accepted proposal/design/delta specs, current CLI config/schema/persistence, create/remove hook planners/executors, doctor, dry-run/JSON result types, docs/skills aggregate registries, and repository `AGENTS.md` files; record the exact owning modules and established test/aggregate commands without editing production or guidance.
- [x] 1.2 Create an implementation evidence ledger mapping issue acceptance criteria AC-01 through AC-12 to delta requirements, the RED tasks in section 2, the later GREEN tasks, owning repository, focused command, native CI job where applicable, and final aggregate; reject any criterion without an explicit pre-implementation RED.
- [x] 1.3 Capture a clean baseline for CLI focused hook/config/schema tests, docs aggregate, skills source/package aggregates, and meta `contracts:check`; distinguish existing failures from feature RED and do not treat missing dependencies, malformed fixtures, or unrelated failures as valid RED evidence.

## 2. Pre-implementation RED gates for every issue acceptance criterion

- [x] 2.1 **AC-01 RED — typed config/schema/persistence:** before production edits, add failing CLI tests for exported root/repository hook types, string-to-Bash runtime normalization, interpreter maps, generated closed JSON Schema at both owner paths, config version remaining exactly `1.0.0`, deterministic schema freshness, and semantically equivalent config load/save/update round trips preserving `hooks.timeout`, scripts, repository fields, and unrelated data.
- [x] 2.2 **AC-02 RED — invalid values before discovery/mutation:** add table-driven failing tests for unknown lifecycle names, dynamic `<lifecycle>.<repo>` keys, empty/whitespace strings, empty maps, unsupported interpreter keys, non-string map members, arrays, numbers, booleans, and null; inject repository-discovery, hook-discovery, interpreter, Git, filesystem-mutation, and process sentinels and prove none is reached, including one-document structured JSON configuration failures with snippet canaries absent.
- [x] 2.3 **AC-03 RED — configured create parity:** add failing real temporary configured-workspace tests for root and repository inline pre/post-create at the exact workspace-once and materialized-repository boundaries, cwd/context/order/multiplicity, repository selection order, pre-mutation rejection, nonzero/timeout failures, and existing owned rollback plus warning retention.
- [x] 2.4 **AC-04 RED — configured remove parity:** add failing real-workspace tests for repository/workspace inline pre/post-remove in per-target repository → workspace → global-targeted → global-shared order, confirmation/destructive gate, target-consistent cwd/context, workspace once-per-target multiplicity, partial operation failure, post-remove finalization, and retention of every operation/hook failure.
- [x] 2.5 **AC-05 RED — repository nesting only:** add focused normalization/schema/type fixtures proving `repos.<name>.hooks.<lifecycle>` is accepted while encoded `hooks.scripts["pre-create.<repo>"]`/`post-create.<repo>` and every unknown dynamic key fail before discovery; prove no second inline form appears in generated schema or contracts.
- [x] 2.6 **AC-06 RED — deterministic interpreter/runtime-doctor parity:** add failing production-resolver tests for POSIX ordered non-empty `PATH` scanning and absolute-realpath Bash selection; Windows fixed `%SystemRoot%` PowerShell/cmd paths plus ordered non-empty `PATH` scanning for `bash.exe`; PowerShell → cmd → Bash fallback, missing/invalid `%SystemRoot%`, empty entries, unavailable higher-priority variants, no `pwsh`/alias/terminal fallback, and actionable `interpreter_unavailable`; add runtime/remove-dry-run/doctor parity fixtures that consume the same injected environment evidence and doctor process sentinels proving non-execution.
- [x] 2.7 **AC-07 RED — same-location ambiguity:** add failing create, remove, remove-dry-run, doctor, human, and JSON fixtures for root-inline/workspace-file, repository-create-inline/repository-specific-workspace-file, and repository-remove-inline/repository-local-file collisions; prove failure precedes lifecycle mutation, identifies both source kinds/owner/file path, executes neither, and leaves different-scope composition intact. Assert create `CREATE_FAILED`, remove `HOOK_CONFIGURATION_INVALID`, outcome `validation_failed`, doctor `HOOK_AMBIGUOUS`, and the exact doctor detail keys.
- [x] 2.8 **AC-08 RED — source-neutral flags/input/timeout/quiet/JSON/dry-run/outcomes:** add failing matrices for create-only `--no-hooks`, shared `--no-hook-input`, TTY attribution before input, unavailable/JSON immediate EOF, valid/default/expired timeout, JSON-owned quiet/progress suppression, one-document JSON isolation, remove dry-run source previews, configured-create dry-run no discovery/empty ledger/no preview, evaluated-prefix create outcomes, and complete remove outcomes. Assert generated help/command contracts do not add `--no-hooks` to remove and use process/discovery/mutation sentinels to prove no fabricated dry-run execution outcomes.
- [x] 2.9 **AC-09 RED — source metadata with no disclosure:** add failing type/human/JSON/preview/doctor/interactive-banner tests for exact `sourceKind`, `sourceOwnerKind`, `sourceOwnerName`, file `sourceScriptPath`, inline null path, repository-create `pre-create.<repo>`/`post-create.<repo>` names, plain remove names, canonical success/failure envelope locations, and exact stream preservation after the additive fields; seed unique snippet canaries and assert they and derived/hash/truncated forms are absent from stdout, stderr, banners, messages, logs, errors, outcomes, previews, findings, environment metadata, and persisted state.
- [x] 2.10 **AC-10 RED — native adapter integration:** before adapter production edits, add failing production-subprocess acceptance for Bash on native POSIX and PowerShell plus cmd on native Windows, including interpreter fallback order, TTY/immediate EOF, spaces, `%`, `!`, `&`, parentheses, fail-fast snippets, exact cwd/environment, timeout/nonzero classification, and one execution only; ensure Windows acceptance runs on a Windows CI job rather than platform injection alone.
- [x] 2.11 **AC-11 RED — all fields and file-only compatibility:** add failing matrix coverage that activates all four workspace and repository fields across create/remove and proves each exact boundary; add unchanged file-only configured and standalone baselines for discovery, ordering, cwd, flags, outcomes, dry-run, and doctor so implementation cannot regress native-file behavior.
- [x] 2.12 **AC-12 RED — CLI docs, website exports, packaged skill, and coordinated contracts:** before any production or prose/export edit, add a failing generator/freshness test for `contracts/inline-lifecycle-hooks.json` schema version `1` from `scripts/contracts/inline-lifecycle-hooks.ts`, assert config version `1.0.0` and command-contract schema version `7`, then add focused failing semantic checkers/fixtures for CLI-maintained docs, docs canonical/generated guidance, skills authored/extracted-package guidance, and normalized cross-repo semantics; register each checker through existing fail-closed aggregates, record source/package RED, add controlled mismatches and aggregate-reachability failures, and leave workflow YAML unchanged unless this RED proves a real topology gap.
- [x] 2.13 Review the AC-01..AC-12 ledger and captured focused failures; confirm every RED loads the intended production/checker path and fails for missing approved behavior, then freeze RED evidence before any task in sections 3 or 4 changes production, docs prose, generated exports, or packaged guidance.

## 3. CLI configuration, resolution, and execution GREEN

- [x] 3.1 Implement shared exported inline-hook lifecycle/interpreter/value types and closed normalization for root `hooks.scripts` and `repos.<name>.hooks`, normalizing shorthand internally while preserving semantically equivalent persisted values and rejecting all AC-02/AC-05 values through canonical `ConfigError` handling.
- [x] 3.2 Extend every config load/save/update path and deterministic JSON Schema generation/artifact so root timeout/scripts, repository hooks, and unrelated fields round-trip semantically while config version remains `1.0.0`; generate `contracts/inline-lifecycle-hooks.json` schema version `1` from its dedicated producer without changing command-contract schema version `7`, and make AC-01/AC-02/AC-05/AC-12 freshness tests GREEN.
- [x] 3.3 Implement one side-effect-free logical-location resolver whose serializable projection excludes snippet text; map configured create/remove inline owners to exact existing file locations and outward logical names, detect file/file and inline/file ambiguity with the fixed command/outcome/doctor classifications, preserve different-scope composition, bypass discovery/preflight only for create's `--no-hooks` after config validation, and prove remove does not gain that option.
- [x] 3.4 Implement shared absolute-path interpreter resolution using the exact POSIX `PATH`, Windows `%SystemRoot%`, and Windows Bash `PATH` policy; preserve PowerShell → cmd → Bash availability fallback, ignore empty/untrusted alternatives, and return actionable non-secret `interpreter_unavailable` for runtime, remove dry-run, and doctor.
- [x] 3.5 Extend the production executor with direct-argument inline `bash -c`, `powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -Command`, and hardened `cmd.exe /d /e:on /v:off /s /c` adapters while reusing timeout, process settlement, TTY/immediate-EOF, output capture, attribution, quiet, signal, and cleanup behavior and never logging snippet-bearing argv.
- [x] 3.6 Integrate resolved inline alternatives into configured create's existing workspace pre, per-repository pre/post, and workspace post slots without a second pass; preserve exact cwd/context/order/multiplicity, preflight-before-mutation, failure, rollback, move/switch/launch boundary, and complete evaluated ledger.
- [x] 3.7 Integrate resolved inline alternatives into configured remove's per-target repository/workspace slots while retaining user-global files, confirmation, all-target preflight, pre-remove destructive gate, post-remove partial-failure finalization, target context, ordering, and complete operation/hook failure retention.
- [x] 3.8 Update hook environment construction so file sources retain absolute `ARASHI_HOOK_SOURCE_PATH`, inline sources omit it, and no snippet-bearing replacement is introduced; preserve every existing execution/target/aggregate/compatibility field and reserved-metadata protection.
- [x] 3.9 Extend create/remove human results, interactive attribution, JSON success/failure ledgers, and remove dry-run previews with exact non-secret source kind/owner metadata and inline null source path; preserve canonical envelope discriminants, ordering, logical names, reason/duration vocabulary, exact stream bytes, configured-create dry-run's empty ledger/no preview, JSON-owned quiet behavior, and single-document stdout.
- [x] 3.10 Route doctor configured create/remove hook checks through the shared resolver and interpreter preflight, add stable blocking ambiguity/unavailability findings, and prove doctor never executes/materializes snippets or discloses snippet text; preserve standalone and file-only diagnostics.
- [x] 3.11 Make all CLI focused RED suites GREEN, then run native POSIX and Windows production-adapter integration including all lifecycle fields/metacharacters/input/timeout/failure, the unchanged file-only suite, config/schema/contract freshness, format, lint, typecheck, build, and complete CLI tests after the final CLI source edit.

## 4. Companion guidance and stable semantic aggregates GREEN

- [x] 4.1 Update CLI README/maintained hook and configuration docs to explain canonical ownership/value examples, inline-versus-file choice, exact executable lookup, ambiguity classifications, exact lifecycle parity, create-only `--no-hooks`, shared input/timeout, JSON-owned quiet behavior, command-specific JSON/dry-run/outcomes, shell-native environment/fail-fast syntax, standalone/file-only boundary, and no-secrets/no-disclosure policy; make the pre-existing CLI semantic RED GREEN.
- [x] 4.2 Update the smallest canonical website hook/configuration/automation pages, preserve outcome-oriented copy and correct cross-links, regenerate Markdown/LLM exports from canonical sources, and make focused freshness/semantic checks plus the stable docs aggregate GREEN without feature-specific workflow steps.
- [x] 4.3 Update the smallest authored Arashi skill hook/configuration references while keeping `SKILL.md` minimal, then create/verify/extract the canonical release archive and make both stable source and extracted-package aggregates GREEN; confirm maintainer checkers/fixtures are excluded and packaged guidance contains the required semantics.
- [x] 4.4 Implement the registered meta inline-hook contract checker against normalized CLI schema/contract input, docs canonical/generated outputs, and authored/extracted skill guidance; make every controlled mismatch and aggregate-reachability fixture GREEN through `contracts:check` and `contracts:check:ci` without reading real snippets or naming focused child checkers in workflow YAML.
- [x] 4.5 Run each repository's focused checker directly and its stable aggregate, then the complete coordinated local stage set exactly once (docs, skills source, canonical skills package, meta); verify registration set equality, deterministic order, source/package failure propagation, explicit child revisions, and workflow-composition self-tests.

## 5. Separate child PR delivery and exact-head gates

- [x] 5.1 Review and commit only owned CLI files in `repos/arashi`, open a separate CLI child PR with `Tracks corwinm/arashi-arashi#271`, cross-link the proposal/meta and anticipated companion PR set, and require exact-head native POSIX/Windows plus full repository CI/review to pass; do not commit or push from proposal drafting work.
- [x] 5.2 Review and commit only owned docs/export files in `repos/arashi-docs`, open a separate docs child PR with a non-closing issue reference and complete cross-links, and require exact-head canonical generation, stable aggregate, package/deploy, and review gates.
- [x] 5.3 Review and commit only owned authored/package/checker files in `repos/arashi-skills`, open a separate skills child PR with a non-closing issue reference and complete cross-links, and require exact-head source aggregate, canonical archive membership/extraction, package aggregate, publication-boundary, and review gates.
- [x] 5.4 Review and commit only owned OpenSpec/meta checker files in the existing meta proposal branch, keep the existing meta PR open with `Tracks #271`, list all child PRs and validation evidence, and require exact-head registry/workflow/coordinated checks without adding a second meta PR.
- [x] 5.5 Before any merge, verify each child PR is open, non-draft, mergeable, review-clean, and green on its live head; verify workflow-scope prerequisites if any workflow diff exists, then squash-merge CLI, docs, and skills child PRs separately in dependency-safe order and delete their remote feature branches.

## 6. Pre-archive reconciliation and final meta readiness

- [x] 6.1 Verify merged child `main` trees contain the reviewed implementation/guidance/checker artifacts, refresh the coordinated meta branch without rewriting history, and rerun CLI native/repository gates plus the complete docs/skills/meta stable aggregate stage set against actual merged child revisions.
- [x] 6.2 Reconcile every AC-01..AC-12 ledger row with exact command/run evidence, confirm all four workspace/repository fields and file-only compatibility, scan final human/JSON/preview/doctor/package outputs for snippet canaries, and confirm no production, docs, skill, or meta requirement remains unverified.
- [x] 6.3 Mark completed OpenSpec implementation tasks only after their evidence exists; compare `openspec list --json` task totals with this checklist, run `openspec validate support-inline-lifecycle-hook-scripts --strict`, inspect final diffs and review threads, and require the existing meta PR to be open/non-draft/mergeable with exact-head coordinated CI green.
- [x] 6.4 Prepare the final archive/sync update: verify the meta PR is the only PR that will use `Closes #271`, record merged child PR links/revisions and validation evidence, and confirm all tracked pre-archive checkboxes are complete before invoking archive.

After every tracked checkbox above is complete, run `openspec archive support-inline-lifecycle-hook-scripts --yes`, inspect and format the archived/synced specs, replace any generated placeholder purpose, validate each synced capability directly, commit/push only the archive and final child pointers to the existing meta PR, change its sole issue reference from `Tracks #271` to `Closes #271`, re-run exact-head meta gates, merge the meta PR last, verify issue closure, and perform coordinated branch/worktree cleanup. This archive procedure is intentionally not a checkbox: making archive itself a prerequisite task would create a circular incomplete-task archive gate.
